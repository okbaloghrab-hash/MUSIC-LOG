# Assistant Custom Rules & Guidelines

## App Profile
- **App Name**: MUSIC LOG - Slowed & Reverb Studio
- **Platform**: React + Vite SPA

## Custom Project Instructions

### 1. Automatic Version Number Synchronization (Critical Rule)
Whenever you are asked to make any structural, procedural, or visual modification to the application:
1. **Locate current version variables**:
   - `package.json` (the `"version"` attribute)
   - `src/components/SplashEntrance.tsx` (the text `v3.8.5` or similar)
   - `src/App.tsx` (the text `v3.8.5` or similar)
2. **Increment the version**:
   - For example, if the current version is `v3.8.5`, increment it to `v3.8.6` (or `v3.9.0` for major features).
   - Ensure the version matches in **all** three files exactly.
3. **Notify the user** of the new version number in your final response.

---
*This file is read automatically by the system to customize your behavior. Always follow this file's guidelines strictly.*
